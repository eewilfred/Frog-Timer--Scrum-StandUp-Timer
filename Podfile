# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Frog Timer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Frog Timer

pod 'CountdownLabel'
  target 'Frog TimerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Frog TimerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
